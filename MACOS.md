# 妙妙工具 · macOS 手动截图版

## 使用

当前 1.3.1 更新版：先退出旧版，再双击 `outputs/妙妙工具-实托邦.app`，
也可双击仓库根目录的 `启动妙妙工具.command`。
应用包已包含 Python 和依赖，使用应用包不需要另外安装 Python。

1. 将完整游戏截图保存为 PNG 或 JPEG，保留完整地图区域。
2. 点击“选择截图并识别”（快捷键 ⌘O），默认自动读取截图顶部层数并切换下拉框。
3. 选择图片后在本机完成识别，检查节点与候选结果，按需手动校正。
4. “手动编辑”页仍可独立使用，支持基底选择和节点编辑。

macOS 版不启用实时截屏，不申请屏幕录制权限，也不会上传图片。
文件选择器默认打开“图片”目录，本次运行中会记住上次选择的文件夹。
支持 PNG、JPEG、BMP；HEIC 请先用“预览”导出为 PNG。

自动层数识别支持游戏顶部金色 `(I)` 到 `(V)` 标记，不依赖网络或 OCR 服务。
截图缺少标题、文字被遮挡或无法可靠判断时，沿用下拉框层数并在底部提示核对。
需要手动指定时，取消勾选“自动识别层数”，选择层数后重新导入图片。

## 实托邦识别

- 从截图顶部识别理念与方针，内置当前汇总的 10 种理念、4 种方针图标。
- 右侧“理念 / 方针”按钮可查看各阶段效果、手动改选；截图未确定强度，不自动猜早中晚期。
- 淡紫虚线框表示疑似受影响节点，是雾区的视觉估计，不是精确边界；可能误报或漏报。
- 紫环和“理想源”文字标记源节点。检测已移除只限第 2 层的限制，要求紫色图标与周围放射花纹共同出现。
- 右键节点可切换“实托邦范围节点”“理想源”，支持撤销与恢复。
- “希望的沃土”为不可消除的正面例外，不套用理想源的紧急作战推断。
- 多片分离雾区都会保留，不把它们自动等同于多个理想源。

测试图 `IMG_3035.PNG`：去温栏 + 修正，理想源位于第 4 行、第 6 列。
`IMG_3034.PNG` 未检出实托邦图标，不添加雾区标记。
详细来源与验证边界见 `outputs/实托邦识别说明.md`。

## 从源码运行

在项目根目录执行（需要 Python 3.11 或更高版本）：

```sh
python3 -m venv .venv
.venv/bin/python -m pip install -r MiaomiaoTools-GitHub/requirements-macos.txt
.venv/bin/python MiaomiaoTools-GitHub/app.py
```

## 测试与打包

```sh
.venv/bin/python -m pip install pytest pyinstaller
cd MiaomiaoTools-GitHub
../.venv/bin/python -m pytest -q
cd ..
./build_macos.command
```

构建输出位于 `outputs/妙妙工具-实托邦.app`，中间文件位于 `work/`。
默认生成与构建机架构一致的应用，Apple Silicon 和 Intel 需要分别构建验证。
当前打包依赖要求 macOS 14 或更高版本（NumPy 的 Apple Silicon wheel 要求）。
本次构建的精确依赖版本保存在 `MiaomiaoTools-GitHub/requirements-macos-lock.txt`，
复现环境时可用该文件替代 `requirements-macos.txt` 安装。
此为本地构建，使用临时签名，未进行 Apple Developer ID 签名和公证。

识别模型、地图模板、图标和手动编辑基底随包附带；回归测试截图不打包。
上游素材与许可证说明保留在 `MiaomiaoTools-GitHub/NOTICE.md` 和应用资源目录中。

## 本次验证

1.3.0 修复 macOS 中文字体回退，第三层特殊“险路恶敌”和第二层理想源回归均通过。
自动层数测试覆盖第 1–5 层实图、缩小截图、连续导入、标题缺失与手动指定；
节点预测测试覆盖居民移动半径、起点缺失、理想源排除、数量约束与设置撤销。

应用包通过签名完整性检查及 Qt 离屏启动验证；用户已确认初版可以手动启动。
构建环境：Apple Silicon（arm64）、macOS 26.2、Python 3.12.13。
macOS 14/15 和 Intel Mac 尚未实机验证。

## 节点预测（1.3.0）

右侧“居民移动次数”针对当前截图设置：未知为默认值，0 表示刚入层，正数为拍摄前发生的移动次数（不是消耗的行动力）。导入新截图后重置为未知，可撤销设置。

- 起点只来自匹配的地图模板或手动指定，不再将当前位置作为原始起点。起点未知时暂停距离预测，摘要提示手动校正。
- 据点候选结合项目现有楼层/起点距离规则，排除流窜居民和已确认的理想源。没有居民也不等于没有据点。
- 移动次数为 n 时，用沿连线 5+n 步作为必要条件；次数未知时不使用居民半径排除节点。这个范围仅用于保守筛选，不模拟居民的完整移动轨迹，也不表示据点必然存在。
- 仅明确为初始截图且模板匹配无需复核时启用全图最低生成数量约束；途中截图保留上限筛选，避免已完成节点变为空地造成错误补足。数量约束冲突时显示提示。
- 尚不自动关联连续截图；重新导入后需重新设置预测条件，不将同模板的不同局合并。项目原有距离表和模板库仍需持续核实，尤其三层据点的本地规则与 Wiki 概述存在差异，未擅自改表。

居民初始生成与移动规则来源：[PRTS 事件节点](https://prts.wiki/w/沉沦者的黑流树海#事件节点)。

开发远程：`origin` 为 `git@github.com:Harry2020rookie/MiaomiaoTools.git`；`upstream` 为原仓库。

## 1.3.1 切层与移动修复

识别页和手动编辑页在本次运行中分别保留各楼层地图及撤销/重做记录。自动识别切层也会保留上一层；清空按钮只清空当前层，同层重新导入会替换该层。关闭应用后不保留，本版本尚未提供跨启动存档。

修复移动当前位置后重绘标记触发异常的问题。
